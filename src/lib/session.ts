// TODO(02-auth): Encrypted httpOnly cookie session via `jose`
// (SESSION_SECRET). Holds the GitHub user access token, refresh token,
// installation id, repo name, and LLM provider preference.
export {};
